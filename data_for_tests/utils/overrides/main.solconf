from soleil.utils import infer_solconf_module
from soleil.resolvers.ref import call

symbol: noid = "Omega"
color = submodule(".color", "red")
filename = call(lambda: f"{symbol}/{id_str() or 'default'}.yaml")
