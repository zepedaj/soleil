_::extends('basic'):

  # (Injected var x_ contains the 'b' node from basic.yaml.)
  # Extends the types in basic.yaml's 'b' node
  b:types(x_)+(float,): 

  # Overwrites the type in basic.yaml's 'c' node:
  c:float: 3.0 

  # Overwrites modifiers of node 'd' in basic.yaml, but
  # keeps the raw value:
  d::noop: $: raw_value(child(x_))
